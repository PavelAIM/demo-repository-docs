#!/bin/bash
set -e
echo "This ZIP archive contains 'dev' and 'prod' setups for OpenWebUI using api.ai-mediator.ru."
echo "To start:"
echo "  cd dev && docker compose up -d"
echo "  cd prod && docker compose up -d"
